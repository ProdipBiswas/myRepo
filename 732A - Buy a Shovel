#include<iostream>
using namespace std;
int main()
{
    int k,r,count=1;
    cin>>k>>r;
    while(1)
    {
        if(k*count%10==0)
            break;
        else if((k*count-r)%10==0)
            break;
        count++;
    }
    cout<<count<<endl;
    return 0;
}
