#include <iostream>
using namespace std;

int main()
{
    long long int k, n, w;

    cin >> k >> n >> w;

    if (w * (w + 1) * k / 2 >= n)
        cout << w * (w + 1) * k / 2 - n << endl;
    else
        cout << 0 << endl;

    return 0;
}
