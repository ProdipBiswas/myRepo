#include <iostream>
using namespace std;

int main()
{
    int n, p, q, i, count;
    i = 1;
    count = 0;
    cin >> n;
    while (i <= n)
    {
        cin >> p >> q;
        if (p + 2 <= q)
        {
            count++;
        }
        i++;
    }

    cout << count << endl;

    return 0;
}
